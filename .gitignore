__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
.env

# Runtime audit logs — never commit captured command history
logs/

# Mark's REAL device inventory — never commit his actual MAC/IP whitelist.
# Only whitelist.example.yaml (placeholder MACs) is committed.
whitelist.yaml
